# piejfx-ai-plugin

## Installation
Extract the archive's contents (the parent folder) under `<PIE_USER_DATA_FOLDER>/plugins`.

`PIE_USER_DATA` is located inside the `AppData/Local` folder on Windows.
